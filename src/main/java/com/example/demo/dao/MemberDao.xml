<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.dao.MemberDao">
	<select id="login" resultType="Member">
		select * from `member`
		left join auth on `member`.authLevel = auth.authLevel 
		where ID = #{ID}
	</select>
	
	<insert id="signup" useGeneratedKeys="true" keyProperty="uid">
		insert into `member` set
		ID = #{ID}, PW = #{PW}, nickname = #{nickname}, email = #{email}, phoneNo = #{phoneNo}
	</insert>
	
	<select id="getMember" resultType="Member">
		select * from `member`
		left join auth on `member`.authLevel = auth.authLevel
		where 1
		<choose>
			<when test="type=='uid'">
				and uid = #{itemValue}
			</when>
			<when test="type=='ID'">
				and ID = #{itemValue}
			</when>
			<when test="type=='nickname'">
				and nickname = #{itemValue}
			</when>
		</choose>
	</select>
	
	<update id="update">
		update member set PW = #{PW}, nickname = #{nickname}, email = #{email}, phoneNo = #{phoneNo}
		where uid = #{uid}
	</update>
</mapper>